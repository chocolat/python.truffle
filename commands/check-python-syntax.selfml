(trigger
    (key [[ctrl][shift]V]))

(name [Validate Syntax (PyCheckMate)])

(only-in source.python)

(input nothing)

(output html)

(save file)

(script [TPY=${TM_PYTHON:-python}

"$TPY" "$TM_BUNDLE_SUPPORT/bin/pycheckmate.py" "$TM_FILEPATH"])